apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: nginx-deamonset
  labels:
    env: demo
    app: nginx
spec:
  
  selector:
    matchLabels:
       env: demo
  template:
    metadata:
      labels:
       env: demo
    spec:
      containers:
      - name: nginx
        image: nginx:1.14.2
